import React from "react"
import { graphq, useStaticQuery } from "gatsby"

const Card = () => {
  return (
    <div className="card-box">
      {/* <p>{info.wordpress.pages.edges.node.content}</p> */}
    </div>
  )
}

export default Card
